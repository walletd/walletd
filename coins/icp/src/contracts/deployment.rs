pub struct CanisterDeployment;

impl Default for CanisterDeployment {
    fn default() -> Self {
        Self::new()
    }
}

impl CanisterDeployment {
    pub fn new() -> Self {
        Self
    }
}
