#[derive(Debug, Clone)]
pub struct CanisterInterface;

impl Default for CanisterInterface {
    fn default() -> Self {
        Self::new()
    }
}

impl CanisterInterface {
    pub fn new() -> Self {
        Self
    }
}
