/**
 * @param {number} count
 * @returns {Uint8Array}
 */
export function bytes(count: number): Uint8Array;
/**
 * @param {number} count
 * @returns {Promise<Uint8Array>}
 */
export function bytesAsync(count: number): Promise<Uint8Array>;
